name: Issue2PR

on:
  issues:
    types: 
      - opened

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: ZanSara/issue2pr@main
        with:
          issue_id: ${{ github.event.issue.node_id }}
    